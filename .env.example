NODE_ENV=development
PORT=3000
APP_NAME=NestJS API

# Use a comma-separated allow-list in production, e.g. https://app.example.com
CORS_ORIGIN=*

# Request limits per client
THROTTLE_TTL=60000
THROTTLE_LIMIT=100
